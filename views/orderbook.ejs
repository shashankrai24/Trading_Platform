<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
</head>

<body>
    <h2 style="text-align: center; margin-top: 10px; margin-bottom: 10px;">Order-book of <%=cryptos.instId%></h2>
    <table class="table">
        <thead>
          <tr>
            <th scope="col">Index</th>
            <th scope="col">Ask-Price</th>
            <th scope="col">Ask-quantity</th>
            <th scope="col">Bid-price</th>
            <th scope="col">Bid-quantity</th>
          </tr>
        </thead>
        <tbody>
           <% 
            counter=0
           crypto['asks'].forEach(function(cryptos)
           {
               counter++;
            %><tr>
            <th scope="row"><%=counter%></th>
            <td><%=cryptos[0]%></td>
            <td><%=cryptos[1]%></td>
            <%
               if(crypto['bids'].length>=counter && crypto['bids'][counter-1].length>=1)
               {%>
                  <td><%=crypto['bids'][counter-1][0]%></td>
               <%}
               else
               {%>
                <td>-</td>
               <%}

            %>
            <%
               if(crypto['bids'].length>=counter && crypto['bids'][counter-1].length>1)
               {%>
                  <td><%=crypto['bids'][counter-1][1]%></td>
               <%}
               else
               {%>
                <td>-</td>
               <%}

            %>
          </tr> 
            <%})
             
            %>
        </tbody>
      </table>
</body>
</html>